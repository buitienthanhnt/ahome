# install the package need run:
#
#
# publish global config:
# publish inertiaJs:
# artisan vendor:publish --provider="Thanhnt\Ahomeglobal\AhomeglobalProvider" (dòng này sẽ chạy cho cả 2)
#
# artisan vendor:publish --tag=ahomeglobal-inertiajs (dòng này sẽ chỉ chạy cho publish inertiaJs)