# function return type: https://dev.to/karleb/return-types-in-php-3fip

# demo for define return type array of function(như này thì trình soạn thảo sẽ nhắc các thuộc tính cho mình)
# 	/**	
#     * @param int $paper_id
#     * @return array{type: string, key: int, value: string, paper_id: int}
#	  * @return array{type: string, key: int, value: string, paper_id: int}[]
#     */
#    protected function convertRequestData(int $paper_id): array